# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# ── Existing example_usage.py ──────────────────────────────────────────────
TENANT_ID=your-tenant-id-here
DATA_AGENT_URL=https://<workspace>.api.powerbi.com/v1.0/myorg/aiassistant/<agent-id>/openai

# ── example_managed_identity_agent.py ─────────────────────────────────────
# Azure AI Foundry project endpoint
# Found in: AI Foundry portal → Project → Overview → "Azure AI Foundry endpoint"
FOUNDRY_PROJECT_ENDPOINT=https://<your-hub>.api.azureml.ms/workspaces/<workspace-id>

# Name of the model deployment inside your Foundry project
FOUNDRY_MODEL_DEPLOYMENT_NAME=gpt-4o
